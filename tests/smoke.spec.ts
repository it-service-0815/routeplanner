import {test,expect} from '@playwright/test'; test('route planner core flow',async({page})=>{await page.goto('/'); await page.getByRole('button',{name:'Runde'}).click(); await expect(page.getByText('25')).toBeVisible(); await page.getByRole('button',{name:'Woche'}).click(); await page.getByRole('button',{name:'Apotheke vorziehen'}).click(); await expect(page.getByText(/Gelbe Warnung/)).toBeVisible(); await page.getByRole('button',{name:'Übernachtungen prüfen'}).count();});
