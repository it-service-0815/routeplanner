export interface Coordinates{lat:number;lng:number} export interface RoutingService{geocode(address:string):Promise<Coordinates>;getTravelTimeMatrix(locations:Coordinates[]):Promise<number[][]>;optimizeDay(input:unknown):Promise<unknown>;optimizeSalesCycle(input:unknown):Promise<unknown>}
export class MockRoutingService implements RoutingService{async geocode(_:string){return {lat:50.197, lng:8.8}} async getTravelTimeMatrix(xs:Coordinates[]){return xs.map((a)=>xs.map(b=>Math.round(Math.sqrt((a.lat-b.lat)**2+(a.lng-b.lng)**2)*95+8)))} async optimizeDay(i:unknown){return i} async optimizeSalesCycle(i:unknown){return i}}
